import { trpc } from "@/lib/trpc";
import { Gavel, Medal, Sparkles } from "lucide-react";

export function HackathonLeaderboard({ hackathonId }: { hackathonId: number }) {
  const { data: rows, isLoading } = trpc.hackathons.leaderboard.useQuery({ hackathonId });
  return <section className="mt-6 border border-[#d7ddd0] bg-[#fcfbf7] p-5"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[#4f674c]">Decision leaderboard</p><h2 className="mt-2 font-serif text-[27px] text-[#1b3829]">Human scores rank the work. AI evidence informs the review.</h2></div><span className="max-w-xs text-right text-[10px] leading-4 text-[#6b766d]">Only finalized human scorecards affect the order. The Hackathon Agent’s preview is never an award decision.</span></div>{isLoading ? <p className="mt-5 text-[11px] text-[#758077]">Calculating finalized human scores…</p> : rows?.length ? <div className="mt-5 overflow-x-auto"><div className="min-w-[700px]"><div className="grid grid-cols-[50px_minmax(220px,1fr)_120px_130px_150px] border-y border-[#d7ddd0] bg-[#f4f4ed] px-3 py-2 text-[9px] font-bold uppercase tracking-[.12em] text-[#758077]"><span>Place</span><span>Project</span><span>Human score</span><span>Final judges</span><span>Agent preview</span></div>{rows.map((row, index) => <div key={row.projectId} className="grid grid-cols-[50px_minmax(220px,1fr)_120px_130px_150px] items-center border-b border-[#e4e7e0] px-3 py-3"><span className="flex items-center gap-1 font-serif text-[18px] text-[#c89412]">{index < 3 ? <Medal className="h-4 w-4" /> : null}{row.humanScore === null ? "—" : index + 1}</span><span><b className="block text-[12px] text-[#304536]">{row.title}</b><small className="text-[10px] text-[#758077]">{row.submittedAt ? "Submitted evidence" : "Draft evidence"}</small></span><span className="flex items-center gap-1 text-[12px] font-bold text-[#1b5e3a]"><Gavel className="h-3.5 w-3.5" />{row.humanScore ?? "Awaiting"}</span><span className="text-[11px] text-[#58685b]">{row.finalizedJudgeCount || "—"}</span><span className="flex items-center gap-1 text-[11px] text-[#71601b]"><Sparkles className="h-3.5 w-3.5" />{row.agentPreview ?? "Not run"}</span></div>)}</div></div> : <p className="mt-5 text-[11px] text-[#758077]">Projects will appear here after teams submit their proof. Finalized human scorecards—not prototype polish or AI output—determine the event order.</p>}</section>;
}
