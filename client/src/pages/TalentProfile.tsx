import { StudioShell } from "@/components/StudioShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { BadgeCheck, BrainCircuit, GitBranch, LockKeyhole, Save } from "lucide-react";
import { useEffect, useState } from "react";

const availabilityOptions = ["developer", "designer", "product_manager", "domain_expert", "data_scientist", "researcher", "mentor"] as const;
type AvailabilityRole = (typeof availabilityOptions)[number];

export default function TalentProfile() {
  const { data: profile, isLoading } = trpc.talent.myProfile.useQuery();
  const utils = trpc.useUtils();
  const [bio, setBio] = useState("");
  const [githubUrl, setGithubUrl] = useState("");
  const [gitlabUrl, setGitlabUrl] = useState("");
  const [portfolioUrl, setPortfolioUrl] = useState("");
  const [linkedinUrl, setLinkedinUrl] = useState("");
  const [skillsText, setSkillsText] = useState("");
  const [availabilityRoles, setAvailabilityRoles] = useState<AvailabilityRole[]>([]);
  const [lookingForTeam, setLookingForTeam] = useState(false);
  const [talentConsent, setTalentConsent] = useState(false);
  useEffect(() => { if (profile) { setBio(profile.bio || ""); setGithubUrl(profile.githubUrl || ""); setGitlabUrl(profile.gitlabUrl || ""); setPortfolioUrl(profile.portfolioUrl || ""); setLinkedinUrl(profile.linkedinUrl || ""); setSkillsText((profile.skills || []).join(", ")); setAvailabilityRoles((profile.availabilityRoles || []).filter((role): role is AvailabilityRole => availabilityOptions.includes(role as AvailabilityRole))); setLookingForTeam(profile.lookingForTeam); setTalentConsent(profile.talentConsent); } }, [profile]);
  const save = trpc.talent.saveMyProfile.useMutation({ onSuccess: () => utils.talent.myProfile.invalidate() });
  const links = [["GitHub URL", githubUrl, setGithubUrl, "https://github.com/..."], ["GitLab URL", gitlabUrl, setGitlabUrl, "https://gitlab.com/..."], ["Portfolio URL", portfolioUrl, setPortfolioUrl, "https://..."], ["LinkedIn URL", linkedinUrl, setLinkedinUrl, "https://linkedin.com/in/..."]];
  const toggleRole = (role: AvailabilityRole) => setAvailabilityRoles(current => current.includes(role) ? current.filter(item => item !== role) : [...current, role]);
  return <StudioShell eyebrow="Talent intelligence"><section className="border border-[#d7ddd0] bg-[#e9ede1] px-6 py-8 md:px-8"><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-[#65705c]">Verified contribution, by consent</p><h1 className="mt-2 max-w-3xl font-serif text-[42px] leading-[.98] text-[#153526]">Make capability visible—<em className="font-normal text-[#876e16]">only</em> when people choose.</h1><p className="mt-4 max-w-2xl text-[14px] leading-6 text-[#49594d]">Your profile helps form complementary proof teams. Any talent-intelligence record is separate, explicit, evidence-scoped, and never an autonomous employment decision.</p></section><section className="mt-6 grid gap-6 xl:grid-cols-[1.05fr_.95fr]"><article className="border border-[#d7ddd0] bg-[#fcfbf7] p-5"><div className="flex items-center gap-2 text-[#1b5e3a]"><GitBranch className="h-4 w-4" /><p className="text-[10px] font-bold uppercase tracking-[.15em]">Participant profile</p></div><h2 className="mt-2 font-serif text-[25px] text-[#1b3829]">Contribute context to the right team.</h2>{isLoading ? <p className="mt-5 text-[12px] text-[#758077]">Loading profile…</p> : <div className="mt-5 grid gap-4"><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Relevant skills<input value={skillsText} onChange={event => setSkillsText(event.target.value)} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="e.g., agronomy, data engineering, UX research" /></label><fieldset><legend className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Role availability</legend><div className="mt-2 flex flex-wrap gap-2">{availabilityOptions.map(role => <button type="button" key={role} onClick={() => toggleRole(role)} className={`border px-2 py-1.5 text-[9px] font-bold uppercase tracking-[.08em] ${availabilityRoles.includes(role) ? "border-[#173d2a] bg-[#173d2a] text-white" : "border-[#cbd5c7] bg-white text-[#617064]"}`}>{role.replaceAll("_", " ")}</button>)}</div></fieldset><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Professional context<textarea value={bio} onChange={event => setBio(event.target.value)} className="mt-2 min-h-24 w-full border border-[#cbd5c7] bg-white p-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="What perspective can you bring to a proof team?" /></label><div className="grid gap-4 sm:grid-cols-2">{links.map(([label, value, setter, placeholder]) => <label key={String(label)} className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">{String(label)}<input value={value as string} onChange={event => (setter as (value: string) => void)(event.target.value)} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder={String(placeholder)} /></label>)}</div><label className="flex items-start gap-2 text-[11px] leading-4 text-[#59675d]"><input type="checkbox" checked={lookingForTeam} onChange={event => setLookingForTeam(event.target.checked)} className="mt-0.5 accent-[#1b5e3a]" />I am open to team invitations that match my listed skills and role availability.</label><Button disabled={save.isPending} onClick={() => save.mutate({ bio: bio || undefined, githubUrl: githubUrl || undefined, gitlabUrl: gitlabUrl || undefined, portfolioUrl: portfolioUrl || undefined, linkedinUrl: linkedinUrl || undefined, skills: skillsText.split(",").map(item => item.trim()).filter(Boolean), availabilityRoles, lookingForTeam, talentConsent })} className="h-10 rounded-none bg-[#173d2a] text-[10px] font-bold uppercase tracking-[.12em] hover:bg-[#0e2b1e]"><Save className="mr-2 h-4 w-4" />Save profile</Button>{save.error && <p className="text-[11px] text-red-700">{save.error.message}</p>}</div>}</article><article className="border border-[#d7ddd0] bg-[#f0f1e7] p-5"><div className="flex items-center gap-2 text-[#4f674c]"><BrainCircuit className="h-4 w-4" /><p className="text-[10px] font-bold uppercase tracking-[.15em]">Consent controls</p></div><h2 className="mt-2 font-serif text-[25px] text-[#1b3829]">Keep talent intelligence bounded.</h2><p className="mt-2 text-[12px] leading-5 text-[#647066]">If you opt in, authorized program administrators may attach verified contribution records from approved proof sprints to this profile. Raw activity is not a performance verdict; the evidence and its limitations remain visible.</p><label className="mt-5 flex items-start gap-2 border-y border-[#d7ddd0] py-4 text-[11px] leading-5 text-[#59675d]"><input type="checkbox" checked={talentConsent} onChange={event => setTalentConsent(event.target.checked)} className="mt-1 accent-[#1b5e3a]" />I consent to a restricted, evidence-backed talent profile that may include verified skills and contribution records from the proof sprints in which I participate.</label><div className="mt-5 space-y-3"><div className="flex gap-3"><LockKeyhole className="mt-0.5 h-4 w-4 text-[#70806f]" /><p className="text-[11px] leading-5 text-[#637166]"><b>Access is restricted.</b> Profile evidence is visible only to authorized program and workforce administrators.</p></div><div className="flex gap-3"><BadgeCheck className="mt-0.5 h-4 w-4 text-[#1b5e3a]" /><p className="text-[11px] leading-5 text-[#637166]"><b>Evidence stays attached.</b> Verified skills must reference actual work, a review, or approved telemetry.</p></div></div></article></section></StudioShell>;
}
