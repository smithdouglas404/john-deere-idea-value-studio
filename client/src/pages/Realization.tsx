import { StudioShell } from "@/components/StudioShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { Activity, ArrowRight, ChartNoAxesCombined, ClipboardCheck, Plus } from "lucide-react";
import { useEffect, useState } from "react";
import { Link } from "wouter";

export default function Realization() {
  const { data: opportunities, isLoading } = trpc.opportunities.list.useQuery();
  const utils = trpc.useUtils();
  const [opportunityId, setOpportunityId] = useState<number | null>(null);
  const [category, setCategory] = useState<"evidence_confidence" | "customer_value" | "operating_value" | "technical_execution" | "claim_integrity" | "originality" | "delivery_fit">("evidence_confidence");
  const [label, setLabel] = useState("");
  const [value, setValue] = useState("");
  const [unit, setUnit] = useState("score / 100");
  const [evidence, setEvidence] = useState("");
  useEffect(() => { if (!opportunityId && opportunities?.[0]) setOpportunityId(opportunities[0].id); }, [opportunities, opportunityId]);
  const record = trpc.opportunities.recordIndicator.useMutation({ onSuccess: () => { if (opportunityId) utils.opportunities.detail.invalidate({ opportunityId }); utils.opportunities.list.invalidate(); setLabel(""); setValue(""); setEvidence(""); } });
  const selected = opportunities?.find(item => item.id === opportunityId);
  return <StudioShell eyebrow="Realization and monitoring"><section className="border border-[#d7ddd0] bg-[#e9ede1] px-6 py-8 md:px-8"><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-[#65705c]">Value after the event</p><h1 className="mt-2 max-w-3xl font-serif text-[42px] leading-[.98] text-[#153526]">Don’t announce a winner. <em className="font-normal text-[#876e16]">Track</em> the change.</h1><p className="mt-4 max-w-2xl text-[14px] leading-6 text-[#49594d]">The realization field keeps the original opportunity, experiment evidence, and operating indicators together after a proof sprint. Value does not move unless the evidence is recorded.</p></section><section className="fieldbook-realization-zones mt-6 grid gap-6 xl:grid-cols-[1.05fr_.95fr]"><article className="border border-[#d7ddd0] bg-[#fcfbf7] p-5"><div className="flex items-center gap-2 text-[#1b5e3a]"><ChartNoAxesCombined className="h-4 w-4" /><p className="text-[10px] font-bold uppercase tracking-[.15em]">Portfolio gate</p></div><h2 className="mt-2 font-serif text-[25px] text-[#1b3829]">What is moving—and why?</h2><div className="mt-5 space-y-3">{isLoading ? <p className="text-[12px] text-[#758077]">Loading portfolio…</p> : opportunities?.length ? opportunities.map(item => <Link href={`/opportunities/${item.id}`} key={item.id} className="flex items-start justify-between gap-4 border border-[#e0e4db] bg-[#f9f9f4] p-4 hover:border-[#a9ba9f] hover:bg-[#f5f8f1]"><span><b className="block text-[12px] text-[#314837]">{item.title}</b><small className="mt-1 block text-[10px] text-[#758077]">{item.stage.replace("_", " ")} · {item.status}</small></span><span className="inline-flex items-center gap-2 text-[11px] font-bold text-[#1b5e3a]">{item.confidence}%<ArrowRight className="h-3.5 w-3.5" /></span></Link>) : <p className="text-[12px] text-[#758077]">No opportunity data is visible in this portfolio yet.</p>}</div></article><article className="border border-[#d7ddd0] bg-[#f0f1e7] p-5"><div className="flex items-center gap-2 text-[#4f674c]"><Activity className="h-4 w-4" /><p className="text-[10px] font-bold uppercase tracking-[.15em]">Evidence-backed update</p></div><h2 className="mt-2 font-serif text-[25px] text-[#1b3829]">Record an indicator shift.</h2><p className="mt-2 text-[11px] leading-5 text-[#647066]">The system preserves who recorded the indicator, its unit, and the evidence supplied. It does not infer a value change from activity alone.</p><div className="mt-5 grid gap-3"><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Opportunity<select value={opportunityId || ""} onChange={event => setOpportunityId(Number(event.target.value))} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]"><option value="" disabled>Select an opportunity</option>{opportunities?.map(item => <option key={item.id} value={item.id}>{item.title}</option>)}</select></label><div className="grid gap-3 sm:grid-cols-2"><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Indicator<select value={category} onChange={event => setCategory(event.target.value as typeof category)} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]"><option value="evidence_confidence">Evidence confidence</option><option value="customer_value">Customer value</option><option value="operating_value">Operating value</option><option value="technical_execution">Technical execution</option><option value="claim_integrity">Claim integrity</option><option value="originality">Originality screen</option><option value="delivery_fit">Delivery fit</option></select></label><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Observed value<input value={value} onChange={event => setValue(event.target.value)} type="number" className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="e.g., 72" /></label></div><div className="grid gap-3 sm:grid-cols-2"><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Label<input value={label} onChange={event => setLabel(event.target.value)} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="Pilot signal quality" /></label><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Unit<input value={unit} onChange={event => setUnit(event.target.value)} className="mt-2 h-10 w-full border border-[#cbd5c7] bg-white px-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="score / 100" /></label></div><label className="text-[9px] font-bold uppercase tracking-[.13em] text-[#758077]">Evidence<textarea value={evidence} onChange={event => setEvidence(event.target.value)} className="mt-2 min-h-24 w-full border border-[#cbd5c7] bg-white p-3 text-[12px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="State the experiment, validated source, reviewer, or observed outcome that justifies this update." /></label></div><Button disabled={!opportunityId || !label || !value || !unit || evidence.length < 10 || record.isPending} onClick={() => record.mutate({ opportunityId: Number(opportunityId), category, label, value: Number(value), unit, evidence })} className="mt-4 h-10 rounded-none bg-[#173d2a] text-[10px] font-bold uppercase tracking-[.12em] hover:bg-[#0e2b1e]"><Plus className="mr-2 h-4 w-4" />Record evidence update</Button>{record.error && <p className="mt-2 text-[11px] text-red-700">{record.error.message}</p>}{selected && <p className="mt-3 text-[10px] leading-4 text-[#738176]">You are updating: <b>{selected.title}</b>. The change will appear in its evidence dossier.</p>}</article></section><section className="mt-6 border border-[#d7ddd0] bg-[#fcfbf7] p-5"><div className="flex items-center gap-2 text-[#1b5e3a]"><ClipboardCheck className="h-4 w-4" /><p className="text-[10px] font-bold uppercase tracking-[.15em]">Governed next step</p></div><p className="mt-2 max-w-3xl text-[12px] leading-5 text-[#56655a]">Once the sponsor sees the updated evidence, the decision can be to advance, reshape, pause, stop, incubate, recruit, or productionize. The product preserves the rationale; it does not turn a single contest result into an automatic investment.</p></section></StudioShell>;
}
