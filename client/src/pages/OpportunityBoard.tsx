import { StudioShell } from "@/components/StudioShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, AudioLines, ClipboardCheck, FileText, Plus, Radar, SearchCheck } from "lucide-react";
import { GuidedWorkflowRail } from "@/components/GuidedWorkflowRail";
import { useState } from "react";
import { Link } from "wouter";

const stageStyle: Record<string, string> = {
  signal: "border-[#d8e0d0] bg-[#edf1e5] text-[#4f674c]",
  shaping: "border-[#eedc9e] bg-[#fff6d8] text-[#8f6a08]",
  evidence: "border-[#b9d4be] bg-[#e4f0e7] text-[#1b5e3a]",
  selected: "border-[#cdd8dc] bg-[#e7edf0] text-[#344f59]",
  hackathon: "border-[#bfd0df] bg-[#e4ecf3] text-[#355d74]",
};

export default function OpportunityBoard() {
  const { data: items, isLoading } = trpc.opportunities.list.useQuery();
  const { data: activeWork, isLoading: isLoadingActiveWork } = trpc.hackathons.myCommandEntry.useQuery();
  const utils = trpc.useUtils();
  const create = trpc.opportunities.create.useMutation({ onSuccess: ({ opportunityId }) => { utils.opportunities.list.invalidate(); window.location.assign(`/opportunities/${opportunityId}`); } });
  const [isOpen, setIsOpen] = useState(false);
  const [title, setTitle] = useState("");
  const [problem, setProblem] = useState("");
  const [domain, setDomain] = useState("");
  const [targetUser, setTargetUser] = useState("");

  return <StudioShell eyebrow="Value field">
    <GuidedWorkflowRail work={activeWork?.[0] || null} />
    <section className="relative mt-6 overflow-hidden border border-[#d7ddd0] bg-[#e9ede1] px-6 py-8 md:px-8 md:py-10"><div className="absolute -right-16 -top-16 h-52 w-52 rounded-full border border-[#9baf8d]/40" /><div className="relative grid max-w-5xl gap-6 md:grid-cols-[1.35fr_.65fr]"><div><p className="text-[10px] font-extrabold uppercase tracking-[.16em] text-[#65705c]">Value Fieldbook operating system</p><h1 className="mt-2 font-serif text-[39px] leading-[.98] text-[#153526] md:text-[52px]">Move a field signal to a <em className="font-normal text-[#876e16]">human</em> investment decision.</h1><p className="mt-4 max-w-2xl text-[14px] leading-6 text-[#49594d]">Capture the friction, build the evidence-backed value case, run a controlled proof, and let people—not AI—own the decision and realization record.</p><Button onClick={() => setIsOpen(true)} className="mt-6 h-11 rounded-none bg-[#173d2a] px-5 text-[10px] font-extrabold uppercase tracking-[.12em] hover:bg-[#0f2a1d]"><Plus className="mr-2 h-4 w-4" />Capture opportunity</Button></div><div className="border border-[#c3d0bb] bg-[#f7f8f2] p-5"><p className="text-[9px] font-bold uppercase tracking-[.14em] text-[#4f674c]">How this works</p><h2 className="mt-2 font-serif text-[24px] leading-6 text-[#1b3829]">The journey above opens each live record in sequence.</h2><p className="mt-3 text-[11px] leading-5 text-[#5c6a60]">Use the rail for the active proof. Capture a new opportunity only when a new field friction needs its own evidence chain.</p></div></div></section>
    <section className="mt-6 border border-[#c9d4c7] bg-[#f0f1e7] p-4 sm:p-5"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[#4f674c]">My active proof work</p><h2 className="mt-1 font-serif text-[25px] text-[#1b3829]">Your applications and next action</h2><p className="mt-1 max-w-2xl text-[12px] leading-5 text-[#647066]">Open the exact event, proof record, or evidence review you need. These are your registered proof sprints, not a generic event list.</p></div><ClipboardCheck className="h-5 w-5 text-[#1b5e3a]" /></div><div className="mt-4 grid gap-3 lg:grid-cols-2">{isLoadingActiveWork ? <p className="text-[12px] text-[#647066]">Loading active proof work…</p> : activeWork?.length ? activeWork.map(work => <article key={work.registrationId} className="border border-[#d7ddd0] bg-white p-4"><p className="text-[9px] font-bold uppercase tracking-[.12em] text-[#758077]">{work.registrationStatus} · {work.registrationRole}</p><h3 className="mt-1 text-[14px] font-bold text-[#1b3829]">{work.eventTitle}</h3><p className="mt-1 text-[11px] text-[#647066]">{work.projectTitle ? `${work.projectTitle} · ${work.projectSubmittedAt ? "proof submitted" : "draft proof"}` : "No proof project linked yet."}</p><div className="mt-4 flex flex-wrap gap-2"><Link href={work.nextAction.route} className="inline-flex min-h-9 items-center gap-2 bg-[#173d2a] px-3 text-[9px] font-bold uppercase tracking-[.1em] text-white">{work.nextAction.label}<ArrowRight className="h-3.5 w-3.5" /></Link><Link href={`/hackathons/${work.hackathonId}`} className="inline-flex min-h-9 items-center border border-[#1b5e3a] px-3 text-[9px] font-bold uppercase tracking-[.1em] text-[#1b5e3a]">Open Event HQ</Link></div></article>) : <p className="text-[12px] text-[#647066]">No active proof-sprint registrations are linked to this account.</p>}</div></section>
    <section className="mt-7"><div className="flex items-end justify-between gap-5"><div><p className="text-[10px] font-bold uppercase tracking-[.16em] text-[#7a837b]">Evidence ledger</p><h2 className="mt-1 font-serif text-[28px] text-[#1b3829]">Opportunities in motion</h2></div><div className="hidden items-center gap-4 text-[10px] font-semibold text-[#68756b] md:flex"><span className="inline-flex items-center gap-1"><AudioLines className="h-3.5 w-3.5" />Voice-ready</span><span className="inline-flex items-center gap-1"><FileText className="h-3.5 w-3.5" />Document-backed</span><span className="inline-flex items-center gap-1"><SearchCheck className="h-3.5 w-3.5" />Source-led research</span></div></div>
      <div className="mt-4 overflow-hidden border border-[#d7ddd0] bg-[#fcfbf7]">{isLoading ? <p className="p-6 text-sm text-[#647066]">Loading opportunities…</p> : items?.length ? <><div className="grid grid-cols-[1.5fr_.7fr_.5fr_auto] border-b border-[#e0e4db] bg-[#f4f4ed] px-4 py-3 text-[9px] font-bold uppercase tracking-[.12em] text-[#858d86] sm:px-5"><span>Opportunity</span><span>Stage</span><span>Confidence</span><span /></div>{items.map(item => <Link href={`/opportunities/${item.id}`} key={item.id} className="grid grid-cols-[1.5fr_.7fr_.5fr_auto] items-center gap-3 border-b border-[#e8ebe3] px-4 py-4 last:border-b-0 hover:bg-[#f5f6ef] sm:px-5"><span><b className="block text-[12px] text-[#293d30]">{item.title}</b><small className="mt-0.5 block truncate text-[10px] text-[#748076]">{item.domain || "Open exploration"} · {item.targetUser || "Target user to confirm"}</small></span><span><i className={`inline-flex border px-2 py-1 text-[9px] font-bold uppercase tracking-[.12em] ${stageStyle[item.stage] || stageStyle.signal}`}>{item.stage.replace("_", " ")}</i></span><span className="text-[11px] font-bold text-[#34493a]">{item.confidence}%</span><ArrowRight className="h-4 w-4 text-[#49614f]" /></Link>)}</> : <div className="px-6 py-12 text-center"><Radar className="mx-auto h-7 w-7 text-[#9aa794]" /><p className="mt-3 font-serif text-[22px] text-[#2c4333]">Your field is ready.</p><p className="mt-1 text-[12px] text-[#718076]">Capture the first opportunity to establish a traceable evidence record.</p></div>}</div>
    </section>
    {isOpen && <div className="fixed inset-0 z-50 flex items-end justify-center bg-[#183b2b]/35 p-4 backdrop-blur-[2px] sm:items-center"><div className="w-full max-w-[620px] border border-[#cfd7cb] bg-[#fbfaf6] p-6 shadow-[0_24px_70px_rgba(18,50,35,.28)]"><div className="flex items-start justify-between gap-4"><div><p className="text-[10px] font-bold uppercase tracking-[.15em] text-[#718074]">Field signal intake</p><h2 className="mt-1 font-serif text-[30px] leading-8 text-[#173529]">Name the opportunity.</h2></div><button type="button" onClick={() => setIsOpen(false)} className="text-[12px] font-bold text-[#607067]">Close</button></div><p className="mt-3 text-[12px] leading-5 text-[#647066]">You will be able to speak, upload documents, review the transcript, and approve the research scope on the next screen.</p><div className="mt-5 grid gap-4"><label className="text-[10px] font-bold uppercase tracking-[.13em] text-[#627064]">Opportunity title<input value={title} onChange={e => setTitle(e.target.value)} className="mt-2 h-11 w-full border border-[#cdd6c9] bg-white px-3 text-[13px] text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="e.g., Prevent avoidable setup rework" /></label><label className="text-[10px] font-bold uppercase tracking-[.13em] text-[#627064]">Observed friction<textarea value={problem} onChange={e => setProblem(e.target.value)} className="mt-2 min-h-28 w-full border border-[#cdd6c9] bg-white p-3 text-[13px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="Describe the field problem, its impact, and what makes it important." /></label><div className="grid gap-4 sm:grid-cols-2"><label className="text-[10px] font-bold uppercase tracking-[.13em] text-[#627064]">Domain<input value={domain} onChange={e => setDomain(e.target.value)} className="mt-2 h-11 w-full border border-[#cdd6c9] bg-white px-3 text-[13px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="Customer uptime" /></label><label className="text-[10px] font-bold uppercase tracking-[.13em] text-[#627064]">Affected user<input value={targetUser} onChange={e => setTargetUser(e.target.value)} className="mt-2 h-11 w-full border border-[#cdd6c9] bg-white px-3 text-[13px] normal-case tracking-normal text-[#1b3829] outline-none focus:border-[#1b5e3a]" placeholder="Field service advisor" /></label></div></div><div className="mt-5 flex justify-end gap-3"><Button variant="outline" onClick={() => setIsOpen(false)} className="rounded-none">Cancel</Button><Button disabled={create.isPending || title.length < 5 || problem.length < 20} onClick={() => create.mutate({ title, problemStatement: problem, domain: domain || undefined, targetUser: targetUser || undefined })} className="rounded-none bg-[#173d2a] text-[10px] font-bold uppercase tracking-[.12em] hover:bg-[#0e2b1e]">Create evidence record</Button></div>{create.error && <p className="mt-3 text-[11px] text-red-700">{create.error.message}</p>}</div></div>}
  </StudioShell>;
}
